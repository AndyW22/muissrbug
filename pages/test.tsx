import { CardContainer } from '../styles/TestStyles';

export default function Test() {
  return (
    <div>
      <CardContainer></CardContainer>
      Notice how the curves are different and theres no transition. If you go
      back to the homepage that image will be wrong too. Reload the page and it
      will work.
    </div>
  );
}
